name: Weekly Data Refresh

on:
  schedule:
    - cron: '0 6 * * 1'  # Every Monday at 6am UTC
  workflow_dispatch:       # Allow manual trigger from GitHub UI

jobs:
  refresh:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Download latest ATP CSV
        run: |
          CURRENT_YEAR=$(date +%Y)
          echo "Downloading $CURRENT_YEAR CSV..."
          curl -sSL "https://stats.tennismylife.org/data/${CURRENT_YEAR}.csv" -o "data/${CURRENT_YEAR}.csv"
          echo "Downloaded successfully"
          wc -l data/${CURRENT_YEAR}.csv

      - name: Refresh database (incremental)
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: python scripts/load_data.py --year $(date +%Y)
